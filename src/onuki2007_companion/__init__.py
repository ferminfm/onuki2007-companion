"""Portable command entry points for the companion's finite checks.

The package deliberately calls the established script paths. Those scripts
remain the transparent primary implementations referenced by the manuscript.
"""

__version__ = "0.1.0"
