# Hand-Calculation Standard

Each mechanically checkable topic begins with a visible derivation. It identifies
the objects and held variables, states assumptions and boundary branch, shows
the intermediate equalities, names the fragile sign or factor, and links the
finite code check and its negative control. A command returning zero is evidence
for that stated finite identity only. It does not replace the source, prove a
continuum model, or establish simulation behavior.
