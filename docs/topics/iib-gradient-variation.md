# Section II.B: Gradient Variation

Use `sections/02b_gradient_entropy_energy.tex` for the hand derivation of the
gradient entropy/energy variations in Onuki Eqs. (2.12)--(2.20). The text keeps
held variables, integration-by-parts boundary terms, and `M_n|_T` explicit.

Run `python3 scripts/python/section_iib_gradient_entropy_sympy.py` or the
matching Wolfram script. The temperature-correction removal test is a diagnostic
negative control, not a source model. The checks establish finite local algebra
only; wall and global-transfer maps remain separate.
